package com.chris.javabasics.acmio.io;

public class Note {
    // TODO 全局只能new出一个Scanner对象

    // TODO nextInt()只能读取整数类型的数据，在读取完后把光标放在同一行该数据的后面

    // TODO next() 只能读取字符串而不能读取空格，在读取完后把光标放在同一行该数据的后面

    // TODO nextLine() 读取整行的数据包括空格直到回车结束，在读取完后把光标放在下一行的开头
}
